#Give miners their ranks
    # SimpleElasticAgent modified to interface with the subnet.
        # For pipeline it should check available (experts) for a valid path towards a forward and backward path.
#Specify when and how to restart? 
    # Start a Postlocal optimizer and crash it. See what happens?
